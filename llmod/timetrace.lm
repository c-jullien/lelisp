defmodule
timetrace
files (
timetrace
)
export (
timetrace
#:timetrace:propa
#:timetrace:propd
#:timetrace:stack
)
import
()
include
()

;;; Added automatically, don't type beyond this line.
cpexport (
(timetrace fsubr () (call) () (subr1) (()))
(#:timetrace:propa subr1 ())
(#:timetrace:propd subr1 ())
)
cpfunctions (
(#:timetrace:off subr1)
(#:timetrace:on subr1)
)
cpimport
()
cpenv (
((putprop '#:timetrace:timetraced 't '#:complice:globalvar)
)((putprop '#:timetrace:runtime 't '#:complice:globalvar)
)((putprop '#:timetrace:stack 't '#:complice:globalvar)
))
