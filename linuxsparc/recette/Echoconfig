
#!/bin/sh

UNIX=IUNIX

echo You are running on Sparc
if [ -f /bin/uname ]
then
echo with Unix system $UNIX: `/bin/uname -s` version `/bin/uname -r`_`/bin/uname -v`
echo based on hardware : `/bin/uname -m`
else
echo with Unix system $UNIX: Linux sparc
echo based on processor sparc
fi

