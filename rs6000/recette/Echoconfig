
#!/bin/sh

UNIX=s5

echo You are running on IBM RS/6000
if [ -f /bin/uname ]
then
echo with Unix system $UNIX: `/bin/uname -s` version `/bin/uname -r`_`/bin/uname -v`
echo based on hardware : `/bin/uname -m`
else
echo with Unix system $UNIX: X11R5
echo based on processor rs6000
fi

